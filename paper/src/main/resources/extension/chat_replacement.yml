# ──────────────────────────────────────────────
#           Mist Chat Replacements
# ──────────────────────────────────────────────
# Replaces message content before formatting
# Supports simple text replacements and PAPI placeholders
# ──────────────────────────────────────────────

replacements:
  heart:
    type: STATIC
    match: "<3"
    replace: "<red>❤</red>"

  smile:
    type: STATIC
    match: ":)"
    replace: "<yellow>😊</yellow>"

  grin:
    type: STATIC
    match: ":D"
    replace: "<gold>😃</gold>"

  sad:
    type: STATIC
    match: ":("
    replace: "<gray>☹</gray>"

  wave:
    type: STATIC
    match: "o/"
    replace: "<yellow>👋</yellow>"

  balance:
    type: PAPI
    match: "<bal>"
    processor: "<yellow><papi:player_name>'s has <papi:vault_eco_balance_formatted> Money!</yellow>"
    permission: "mist.replacements.balance"

  location:
    type: PAPI
    match: "<loc>"
    processor: "At <papi:player_x>, <papi:player_y>, <papi:player_z>"
    permission: "" # no permission required

# Used for migrations. DO NOT EDIT.
config_version: 1